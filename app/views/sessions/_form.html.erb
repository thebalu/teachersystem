<%= form_with(model: session, local: true) do |form| %>
  <% if session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(session.errors.count, "error") %> prohibited this session from being saved:</h2>

      <ul>
      <% session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :session_description %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location, id: :session_location %>
  </div>

  <div class="field">
    <%= form.label :when %>
    <%= form.datetime_select :when, id: :session_when %>
  </div>

  <div class="field">
    <%= form.label :course_id_id %>
    <%= form.text_field :course_id_id, id: :session_course_id_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
