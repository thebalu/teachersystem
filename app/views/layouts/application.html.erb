<!DOCTYPE html>
<html>
<head>
  <title>Teachersystem</title>
  <%= csrf_meta_tags %>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
<div class="navbar">
  <% if teacher_signed_in? %>
    Signed in as <%= current_teacher.email %>. <%= link_to 'Sign out', destroy_teacher_session_path, method: :delete %>
  <% else %>
    nop
  <% end %>
</div>

<div class="content">
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
  <%= yield %>
</div>

</body>
</html>
